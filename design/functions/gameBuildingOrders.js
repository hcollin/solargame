
const { randomId } = require("./utils.js");

function build(gameState, player, orderObject) {return gameState;}

function destroy(gameState, player, orderObject) {return gameState;}

function upgrade(gameState, player, orderObject) {return gameState;}

function repair(gameState, player, orderObject) {return gameState;}

module.exports = { build, destroy, upgrade, repair};