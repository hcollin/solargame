
const { randomId } = require("./utils.js");


function adjustBasicResearch(gameState, player, orderObject) {
    return gameState;
}

function selectBreakthroughTarget(gameState, player, orderObject) {
    return gameState;
}

module.exports = {adjustBasicResearch, selectBreakthroughTarget};