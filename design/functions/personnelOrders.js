
const { randomId } = require("./utils.js");

function hire(gameState, player, orderObject) {
    return gameState;
}

function assign(gameState, player, orderObject) {
    return gameState;
}

function fire(gameState, player, orderObject) {
    return gameState;
}

function instruct(gameState, player, orderObject) {
    return gameState;
}

module.exports = {hirePerson: hire, assignPerson: assign, firePerson: fire, commandPerson};

